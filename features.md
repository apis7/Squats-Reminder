[done] quasi-transparency not working if automatically triggered, only manually triggered

[done] for the "change interval" - can we allow users to change the timing interval, AND the snooze intreval? and then, dynamically update the snooze button
(right now it says "3 hours" - obviously that would change if the user changed it.)

[done] is it possible to keep track of and display "how many in a row" user has successfully hit "I did my Squats" button? i.e. a sort of gamified situation? 
which resets if the users snoozes it or closes the program.

[done] I added a few mp3 music snippets to the folder (/music - NOT in the "/Raw Music" subfolder). Can we RANDOMLY play those - let's say there's a 10% chance that one of those given music snippets will play (don't change the thunder, still play the thunder + music) and it randomly chooses which music snippet plays? (also, unlike the thunder, the music snippet doens't repeat, only plays through once and then done). Also, the music fades out if the user clicks any of the buttons - I did my squats, snooze, or the (x). Also, only play at 30% volume. Doable?

[done] On that note: add a menu feature (to the right-click favicon) that allows the user to enable/disable sound alert (sound is enabled by default)

[done] Can you make the favicon shaded-3d looking? And change the green color to slightly more blue-green (similar to the blue lightning color, but make sure it's bright enough - e.g. something LIKE (doesn't have to be exact) #33c2f2) (don't change the overall look/feel - I love it!)
